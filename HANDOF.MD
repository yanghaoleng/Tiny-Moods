# 任务交接

更新时间：2026-08-19

## 当前目标

维护已上线的 `Tiny Moods`：用户上传一张照片，生成九张透明表情、永久互动页和浏览器本机视频；运营后台可查看作品、链接、成本、访问、停留时间与交互埋点。

本轮目标已完成并上线：Seedream 5.0 Pro / Lite 模型选择、匿名 Cookie 生成记录与恢复、完整名字精确查询、作者主页、5 色作品背景及作品主人显示设置持久化。

最新修正：保存方案改为直接提供火山引擎生成的完整九宫格原文件；网页互动继续使用抠图后的轻量 WebP，不再生成下载 JPG 或 ZIP。

本轮继续校准左上角品牌：相机图标单独上移 `10px`，与 `Tiny Moods` 字标保持光学横向对齐。

本地待发布改动：AI 原图保留与直接下载链路已完成本地验收，等待正式发布；GitHub 推送由杨总手动完成。

本次交互精简已随正式版本上线：二维码小窗去掉指向箭头；九图批量保存复用已读取字节并零压缩打包，按钮位于九图列表底部；互动页删除底部重复静音按钮和右下角 `made for` 角标；生成中页面删除底部图标说明。

## 本轮已完成

- [x] 火山引擎返回的图片字节原样写入 `sheet-original.*`，不再旋转、压平或重新编码
- [x] 抠图完成后永久保留原始九宫格，同时保留九张互动用透明 WebP
- [x] 停止为新任务生成白底 JPG 和 ZIP，并移除前端九图读取、系统分享和压缩包预取
- [x] 保存入口改为“保存原图”，弹层只展示完整母图和“下载 AI 原图”主按钮
- [x] 历史作品仅在母图仍存在时提供原图下载，不用抠图后素材伪造 AI 原图

- [x] 真实触摸移动设备完全屏蔽鼠标跟随与右下角静态情绪标签，小尺寸桌面窗口保留桌面交互
- [x] 手机首屏空上传区压缩至约 `150px`，文案精简为“上传照片”，首屏可看到下方案例
- [x] 打赏弹窗去掉“建议打赏”，模型选择精简为默认 Lite 的 Lite / Pro Switch，微信内弹层支持纵向滚动
- [x] 新任务长期只保存九张 1080px 透明 WebP；单张与 ZIP 下载统一为白底 JPG
- [x] 九图并行压缩并在任务完成时预制 ZIP；历史作品首次下载时自动补建，旧 PNG 仍兼容读取
- [x] 新增 `faces:restore-png` 回滚工具，使新 WebP 作品可兼容旧服务版本

- [x] 保存图片半弹窗移除两个操作间的分割线和说明小字，视频入口改为无填充次要按钮“保存视频（测试）”
- [x] 修复左下角作者主页样式前的无效字符，页边距与 NCM 统一为 `18px clamp(16px, 3vw, 36px) 22px`
- [x] 移除首页右上角界面音效静音按钮，只保留“生成记录”，不影响互动页背景音乐开关
- [x] 作品轮播改为 5 种相近浅色背景循环，保留原有扩散换色动效
- [x] 新增作品显示设置接口，作品主人的背景和装饰开关按作品保存，分享访客不能覆盖
- [x] 放大打赏价格下方的感谢文案，改为夸赞用户自愿支持行为的语气
- [x] 安装 `calligraph`，为感谢文案加入逐字入场，并适配减少动效设置
- [x] 保留 10 条简短感谢文案并随机展示，建议打赏改为 `¥1 ~ ¥3`，新增作者微信 `yanghaoleng` 一键复制
- [x] 移除头像按钮 `whileHover`，自动换表情继续由独立的 `AUTO_CHANGE_INTERVAL=6563ms` 驱动
- [x] 默认头像、Hero、App Icon 和全部装饰 PNG 转为 WebP；旧 `favicon.png` 替换为 SVG
- [x] 首页四组案例改为只预取下一张，不再一次预载 36 张 WebP
- [x] 服务端为每张透明 PNG 原图生成 960px WebP 预览；互动浏览、二维码和视频读取 WebP，保存读取 PNG
- [x] 服务启动时补齐历史作品 WebP；图片响应使用 `private, max-age=31536000, immutable`
- [x] 互动页底部直接外露“复制链接 / 保存图片 / 分享二维码”三个操作
- [x] 保存图片改为底部半弹窗，支持一键保存九张、逐张点开和浏览器长按保存
- [x] 保存视频移到保存图片半弹窗底部，并注明“测试功能，生成比较慢，请保持页面打开”
- [x] 分享二维码改为无箭头圆角弹层，1080×1080 高纠错童趣圆点码中央嵌入圆角方形底板与作品表情，图片支持点开或长按保存
- [x] 分享互动页左上角使用 Phosphor 房子图标返回主页，不再使用 App Icon
- [x] 背景音乐页面挂载后立即请求播放，音频改为 `preload="auto"`；浏览器拦截时第一次可信点击补播
- [x] 左上角新增背景音乐开关；背景与装饰开关保留在顶部
- [x] 新增 `audio_play`、`audio_pause`、`qr_open`、`qr_close`、`save_images_close` 埋点中文映射
- [x] Favicon 每次打开从 9 套相机配色中随机选择，与上次不紧邻重复
- [x] 指纹静态资源启用一年强缓存，作品 WebP 继续使用一年私有缓存
- [x] 图片裁剪四边内缩 `0.6%`，生成状态提醒不要关闭页面
- [x] 分享二维码恢复童趣圆点样式，人物头像置于圆角方形底板
- [x] 图片生成成功后直接进入互动页，不再渲染独立成功页面
- [x] 二维码小窗删除方向不准确的气泡箭头
- [x] 九图 ZIP 复用预读字节并使用零压缩打包，显示逐张准备进度；下载按钮移至九图列表底部
- [x] 互动页删除底部重复界面音效按钮和右下角 `made for` 角标
- [x] 图片生成中页面删除最底部图标与保留说明
- [x] 首页空上传框缩至桌面 `320px`、窄屏 `270px`，选图预览同步收短
- [x] 移除首页导航下边线与案例区上边线，压缩两个区块之间的留白
- [x] 名字说明移到输入框上方；输入框、移除照片和生成按钮统一 `54px` 并同排对齐
- [x] 首页随机大标题整体向下移动 `10px`，桌面与手机端保持一致
- [x] 打赏页新增 Seedream 5.0 Pro / Lite 单选，建议金额分别为 `¥0.60` / `¥0.22`
- [x] `/api/orders/donation` 增加模型白名单校验，订单与任务持久化模型档位、模型 ID、尺寸和建议金额
- [x] 生成管线按任务快照调用对应火山模型，并分别记录模型成本估算
- [x] 前后端默认模型统一为 Lite；Lite 对应 `doubao-seedream-5-0-260128`，Pro 对应 `doubao-seedream-5-0-pro-260628`
- [x] 首页左上角 `Tiny Moods` 品牌标题下移 `10px`，按下态保持相同基准位置
- [x] 首页相机图标相对字标上移 `10px`，桌面与手机端共用同一光学对齐规则
- [x] 运营后台记录卡和详情显示友好模型名，自愿打赏显示建议金额而非虚报已支付金额
- [x] 新增模型切换交互埋点 `donation_model`
- [x] 服务端签发一年期 HttpOnly 匿名 Cookie，只保存随机访客 ID，并把新订单、任务归属到当前浏览器
- [x] 首页右上角新增“生成记录”，实时轮询显示当前浏览器的进行中、等待本地处理、已完成和失败任务
- [x] 新增完整名字精确查询，只返回已完成作品，限制频率并最多返回最近 20 个同名结果
- [x] 每个任务创建时写入固定 `?job=:id` 恢复地址；Cookie 所有者重新进入后无需本地令牌也可继续下载母图并完成本地处理
- [x] 记录面板具备桌面双列、手机底部半弹窗、加载、空、错误和键盘关闭状态
- [x] 首页左下角新增与 NCM Studio 同款的“作者主页”小字外链，并记录 `author_home_open` 交互埋点
- [x] 修复完整名字搜索漏掉 `awaiting_client_processing`；待前端处理结果附带恢复凭证并进入固定任务页继续完成

## 本轮验收

- [x] 端到端 DEMO 任务完成后仅保存九张 WebP、原始母图和任务信息，ZIP 数量为 `0`
- [x] 浏览器处理前母图、任务完成后母图、最终下载文件的 SHA-256 完全一致
- [x] 390×844 移动端实测无横向溢出，原图预览、下载按钮和视频按钮均在弹层内可见
- [x] `npm run build` 通过，638 个模块完成构建；服务端三个模块语法检查通过

- [x] 最大实测旧任务九张 PNG 合计 `14.72MB`，对应 WebP `0.86MB`，存储减少约 `94%`
- [x] 同素材旧串行流程约 `4.85s`，新 WebP/JPG/ZIP 并行流程约 `0.49s`，本机基准约快 `9.8x`
- [x] 本地 DEMO 与历史作品均返回九个 JPG 下载地址和 `faces-jpg-v2.zip`；JPG 四角验证为纯白色
- [x] 线上首页、主 JS、主 CSS 与本地构建逐字节一致；DEMO 单图为 `1080×1080` JPG，ZIP 恰好 9 项
- [x] 线上 4 组 DEMO 共保留 36 张 WebP 和 4 个预制 ZIP，PNG 数量为 `0`；服务健康检查通过
- [x] 鼠标轨迹完成逐帧合并、标签尺寸初始化缓存、10 节点池复用、900ms 生命周期和移动箭头滤镜移除
- [x] 本机 Chrome 约 120Hz 指针压力测试：平均帧间隔 `16.67ms`、P95 `17.3ms`、最差 `17.7ms`、超过 `25ms` 的帧 `0`、长任务 `0`；移动期间标签布局读取 `0`、控制台异常 `0`

- [x] 等待前端处理作品的精确搜索恢复链路通过：结果返回固定 `?job` 和恢复凭证；无凭证母图为 `404`，恢复凭证读取为 `200`
- [x] 保存图片半弹窗操作层级调整后 `npm run build` 通过，639 个模块完成构建
- [x] 作者主页页边距修复后 `npm run build` 通过，639 个模块完成构建
- [x] 首页静音按钮移除后 `npm run build` 通过，639 个模块完成构建
- [x] 5 色轮播与作品显示设置持久化改动 `npm run build` 通过，639 个模块完成构建；`server/index.mjs` 语法检查通过
- [x] 打赏感谢文案与 Calligraph 动效改动 `npm run build` 通过，639 个模块完成构建
- [x] `npm run images:optimize` 可重复执行；本次目标图片 `5,953,190 → 2,172,348 bytes`，减少 `64%`
- [x] 示例单张服务端预览 `398,511B PNG → 30,944B WebP`，减少约 `92%`
- [x] `npm run build` 通过，636 个模块完成构建
- [x] 390×844 手机布局：左上角主页与音乐、右上角背景与装饰、底部四项操作均无拥挤或横向溢出
- [x] 1280×800 桌面布局通过；主页为房子图标，三个分享操作显示完整标签
- [x] 鼠标停在头像区域 7 秒后表情继续自动切换
- [x] 浏览器可信点击后 BGM 播放成功；左上角开关可暂停和恢复
- [x] 二维码气泡显示作品表情并输出 PNG Data URL；保存半弹窗显示九张预览、视频按钮和测试提示
- [x] DEMO 接口返回 9 个 WebP `src` 与 9 个 PNG `downloadSrc`，MIME 和一年缓存头正确
- [x] Favicon 100 次逻辑抽测覆盖全部 9 种方案，浏览器连续 6 次刷新均无紧邻重复
- [x] 线上历史作品 63 张 PNG 全部有 WebP 预览，缺失数 `0`；总体积 `40,669,162B → 3,288,262B`
- [x] 线上 `index / JS / CSS / favicon` 与本地构建逐字节一致；HTTPS、后台入口、静态 WebP MIME 与一年强缓存均通过
- [x] 本轮 `npm run build` 通过，636 个模块完成构建；首页关键尺寸、分割线和上传后布局规则已写入指纹 CSS
- [x] `20260819170033` 首页、CSS、JS 与范围收敛后的构建 SHA-256 一致；首页、后台均为 `200`，旧域名保留路径参数 `301` 跳转
- [x] 本轮模型选择改动 `npm run build` 通过，638 个模块完成构建；服务端三个模块语法检查通过
- [x] 本地 DEMO 端到端验证：非法模型返回 `400`；Lite 订单、任务、生成阶段与后台均保持 `lite / ¥0.22 / 2144x2144`；Pro 订单为 `pro / ¥0.60`
- [x] 默认、Lite、Pro 三种下单路径复核通过：默认与 Lite 均使用 `doubao-seedream-5-0-260128`，Pro 使用 `doubao-seedream-5-0-pro-260628`
- [x] 线上 `/api/health` 返回 `defaultImageModel=lite`；首页 JS/CSS 与本地构建 SHA-256 一致，左上角品牌规则含 `translateY(10px)`
- [x] 品牌图标上移后完成 `1280×800` 与 `390×844` 实截图验收；线上指纹 CSS 含 `.generator-brand-mark{transform:translateY(-10px)}`
- [x] 本轮 `npm run build` 通过，638 个模块完成构建；`node --check server/index.mjs` 与 `git diff --check` 通过
- [x] 本地接口串联验证：同一 Cookie 可读取刚创建的进行中任务，历史项返回固定 `?job=:id`，重新读取任务返回 `owned=true`
- [x] `20260819172241` 公网首页与本地 `index / JS` SHA-256 一致；线上 JS 包含 5 色背景、显示设置接口、生成记录、作者主页和二维码错误诊断
- [x] 线上匿名 Cookie 为一年期 `HttpOnly; Secure; SameSite=Lax`；未授权作品设置修改返回 `404`

## 发布状态

- 本轮合并提交为 `ff4679a`，已推送至 GitHub `main`；其中保留 `7815eb5` 移动端与图片链路优化，并合入 `fb23fb3` 品牌图标对齐
- 当前线上发布 `20260819182515`，直接回滚点 `20260819182437`；更早发布继续保留作审计
- `9face.service` 为 `active`，`/api/health` 返回 `ok=true`，公网首页与 `/admin` 均返回 `200`
- API Key、管理密码、会话密钥、埋点盐和运行数据仅保存在服务器环境文件或数据目录，敏感信息扫描已通过，未进入公开仓库

## 当前生产

- 地址：`https://tinymoods.mikeywa.site/`
- 仓库：`https://github.com/yanghaoleng/Tiny-Moods`
- 主机：`115.159.25.169`
- 服务：`9face.service`，监听 `127.0.0.1:8095`
- 程序：`/opt/9face/current`
- 数据：`/var/lib/9face`
- 配置：`/etc/9face.env`

## 关键文件

- 互动页：`src/JennieExperience.jsx`、`src/styles.css`
- 分享与保存：`src/generator/GeneratorApp.jsx`、`src/generator/generator.css`
- 图片预览与接口：`server/index.mjs`、`server/pipeline.mjs`
- 模型档位配置：`server/image-models.mjs`、`.env.example`
- 静态图片压缩：`scripts/optimize-static-images.mjs`
- 本机视频：`src/generator/localVideoRender.js`、`src/remotion/`
- 前端埋点：`src/analytics.js`
- 运营后台：`src/admin/AdminApp.jsx`、`src/admin/admin.css`
