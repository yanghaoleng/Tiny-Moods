# 任务交接

更新时间：2026-08-19

## 当前目标

维护已上线的“九格拍立得”：首页展示四个动态 DEMO；Seedream 只生成无缝纯头部九宫格，拆图、抠图和视频渲染均在用户浏览器完成；全站按钮已接入 `10 Cinematic` 界面音效。

新增状态：杨总已确定最终 `Tiny Moods` 字标并移除实验 Tab；首页主标题加入 Typer 波浪入场和 11 组随机文案，保留本地预览，尚未发布生产。

本轮新增 `/admin` 运营后台与匿名埋点：可查看全部生成记录、九张成图、永久链接、生成参数、成本和逐次事件；页面访问、可见停留及交互采集已本地联调和浏览器验收，尚未发布生产。

相机图标的 9 个正放扁平方案已全部保留；临时 Tab 已移除，每次进入随机出现一个，点击 `Tiny Moods` 品牌区域随机切到另一个且不连续重复。当前只部署本地。

本轮新增桌面端放大黑色鼠标箭头与中英文单语情绪词彩色轨迹，触摸和低动态偏好使用静态降级；尚未发布生产。

分享菜单已新增九张透明图的二级平铺保存页，支持顶部批量保存和单张点开/长按保存；分享二维码已改为无标题的纯黑白方形码。已本地验收，尚未发布生产。

## 本地交互方案

- [x] 首页改为上下布局，加入单图拖拽/点击上传与对象名字输入
- [x] 首页固定为 C“轻日系相册”，移除方案 Tab 和本机主题记忆
- [x] 首页删除上传说明副标题，主标题从 11 句文案中随机选择，不自动轮播，点击标题可换一句
- [x] 案例移动到上传区下方并改为无框微浮动；桌面 3:4 浮窗、手机底部半弹窗预览
- [x] 浮窗预览隐藏互动页返回按钮，统一使用右上角关闭
- [x] 二维码改为 1080×1080 纯黑白方形码，移除标题、头像、说明和海报装饰
- [x] 保存二维码保持主操作，保存视频保持文字次操作
- [x] 分享首层新增“保存 9 张透明图片”单一入口，点击进入二级平铺页
- [x] 二级页按 3×3 显示九张原图，顶部支持批量保存，手机走系统多图保存、电脑下载 ZIP，单张支持点开大图和长按保存
- [x] 打赏与分享弹窗重做为极简黑白
- [x] 打赏弹窗移除产品图标和网站名，标题精简为 `¥0.6`，更新成本说明与继续生成按钮，二维码到按钮间距设为 `20px`
- [x] 背景与装饰开关移到互动页顶部，分享弹窗移除重复开关
- [x] 互动页移除顶部作品标识和造型计数，底部返回入口改为主页产品图标
- [x] 纯二维码与九图平铺页在桌面与 390px 手机端无横向溢出或运行错误
- [x] 真实浏览器验证二维码为 1080×1080，九张 720px 原图全部加载，批量保存按钮可用
- [x] 全站移除系统暗色模式分支，固定明亮主题
- [x] `Tiny Moods` 收敛为统一加粗字号：`Tiny` 黑底，`Moods` 逐字随机配色，色块随字形边界变化
- [x] 移除标题实验 Tab；首页主标题接入随机状态合并的 Typer 波浪入场
- [x] 9 个扁平透明相机图标全部保留；进入随机出现，点击品牌随机切换且不连续重复
- [ ] 杨总确认本地最终效果后再发布生产

## 本轮已完成

- [x] 新增带独立密码和 HttpOnly 会话的 `/admin` 运营后台
- [x] 后台登录取消账户名，仅保留 6 位密码框；兼容手机数字键盘、电脑数字键和回车，本地口令已配置且明文不写入文档
- [x] 后台支持生成状态/关键词/案例筛选、分页、3×3 九图预览、永久链接和单次生成详情
- [x] 单次详情展示模型、尺寸、Seedream 请求号、估算成本、订单摘要、失败详情和事件时间线
- [x] 新增 `page_view`、15 秒可见时长心跳 `page_stay`、离开补报和全局点击/输入状态交互采集
- [x] 核心操作统一稳定动作名，访问/停留/交互按 `jobId` 或 `demoId` 关联到具体作品
- [x] 埋点按北京时间持久化到 `DATA_ROOT/analytics/events/YYYY-MM-DD.jsonl`，不保存原始 IP、完整 User-Agent、输入内容和原照片
- [x] 接口联调验证未登录 401、登录成功、事件写入、聚合和单任务回查
- [x] 生产构建通过（637 个模块）；运营后台独立拆包约 30.32kB JS / 17.84kB CSS
- [x] 浏览器验收后台登录、桌面总览、36 张案例图、详情抽屉和 390px 手机布局；无横向溢出、控制台错误或首页回归问题
- [ ] 在生产 `/etc/9face.env` 配置 `ADMIN_PASSWORD`、`ADMIN_SESSION_SECRET`、`ANALYTICS_SALT`
- [ ] 杨总确认后再将运营后台与埋点发布生产

- [x] 分享菜单新增九图保存入口与 3×3 平铺页；手机调用系统多图保存，电脑一次下载含 9 张原图的 ZIP
- [x] 分享二维码改为无标题、无头像、无说明的 1080×1080 纯黑白方形码
- [x] 生产构建通过（622 个模块）；真实浏览器验收桌面端、390px 手机布局、9 张原图加载与 ZIP 打包成功反馈
- [x] 将杨总选定的 11 句首页文案整理为 `landingHeadlines` 随机词库
- [x] 每次进入或刷新首页先随机一句；标题优先单行，空间不足时仅在第一个逗号后换行
- [x] 长标题自动启用紧凑字号，逗号前后短语保持完整，不在短语内部断行
- [x] 点击首页大标题随机切换文案，不连续重复，并重新播放 Typer 动效
- [x] 标题实际换行时隐藏第一行末尾逗号；单行仍保留逗号，窗口变化时自动重新判断
- [x] 自定义黑色鼠标指针缩至原尺寸的 80%（46px → 37px），轨迹标签尺寸不变
- [x] 新增全站 `EmotionCursorTrail`，桌面隐藏原生指针并显示 `37px` 近黑大箭头
- [x] 情绪词在序列中混用中文和英文，但单枚只显示一种语言；使用洗牌词袋和轮换色板，按时间与移动距离限流
- [x] 轨迹生成前按真实 DOM 尺寸做 AABB 碰撞检测，标签保持 `4px` 空隙且最多同时 `16` 枚
- [x] 页面隐藏时清理轨迹；触摸和低动态偏好显示静态示例
- [x] 指针按压反馈收敛为右下 `2px` 位移，不再明显缩放
- [x] 单枚关键词改为中文或英文单语，并将出现与消失改为更柔和的淡入淡出
- [x] 生产构建通过（637 个模块）；实际浏览器逐条验收 11 句标题，1280px 与 320px 均无横向溢出、无连续重复；换行隐藏逗号、单行保留逗号，窗口缩放可自动恢复，控制台无错误
- [x] 安装 `uisfx@0.4.0`，全站固定使用 `cinematic` 音色包与 `0.56` 总音量
- [x] 按选择、开关、弹层、导航、复制、下载、重试、生成、成功和失败映射语义音效
- [x] 本机视频渲染接入可清理的 `processing` 循环及 `complete` / `error` 结果音
- [x] 增加全站静音按钮，状态持久化；首次可信操作后才解锁和预热 Web Audio
- [x] 音效不使用 hover 触发，不加载外部音频文件；生产构建通过（612 个模块）
- [x] App Icon 旁产品名确定为 `Tiny Moods`，首字母大写并使用统一加粗的 Cormorant Garamond 衬线字标
- [x] `Tiny` 固定黑色阶梯底，`Moods` 每 3 秒逐字随机换色；悬停单字和点击整套换色保留
- [x] 主标题 Typer 通过定时器直接切换字符状态，完成后回到完整粗体文字；所有定时器和观察器均可清理
- [x] 浏览器验证统一字号/粗细、点击换色、3 秒循环、Typer 中间态和最终态、320px 无横向溢出；本地地址 `http://127.0.0.1:4173/`
- [x] 标题动效补齐移动端尺寸与 `prefers-reduced-motion` 静态降级
- [x] 相机图标按现有字标色板重绘为 9 个网页矢量方案，不修改 `public/app-icon.png`
- [x] 移除导航下方临时图标 Tab，恢复单层简洁导航；最近图标编号只记录在当前会话
- [x] 本地 `http://127.0.0.1:4173/` 连续验证 12 次刷新与 10 次点击，均无紧邻重复、无横向溢出，控制台无警告或错误
- [x] 生产构建通过（622 个模块）；本轮随机图标交互尚未发布生产
- [x] 提示词强化为“只要一颗头”，禁止脖子、肩膀、衣服、手和身体
- [x] 九宫格要求严格三等分、零间隙、零边框；前端裁切内缩改为 `0`
- [x] 真实调用 Seedream 重做团子案例，9 张均为透明纯头部
- [x] 清理团子耳机图的连接线，九张输出边缘均无主体碰边
- [x] 打赏标题与说明改为建议 `0.6 元`
- [x] 服务端 Remotion 渲染停用；图片完成后直接进入永久互动页
- [x] 正式作品和三个 DEMO 均改为 Remotion Web Renderer 本机渲染
- [x] 保存视频进入本机进度页，完成后自动下载并提供再次下载按钮
- [x] 真实浏览器渲染下载验证通过：H.264/AAC MP4、720×1280、30fps、21.269 秒、约 6.5MB
- [x] 生产构建通过（609 个模块），视频渲染器保持按需加载
- [x] 永久作品 `cec29c...` 的九张头像替换首页 Jennie 案例
- [x] 永久作品 `0794c...` 新增为“羊石坨坨”案例
- [x] 首页改为四头像错位 2×2 布局；1440px 和 390px 实机浏览器验收无重叠、无横向溢出
- [x] Jennie 与羊石坨坨各九张素材转为 720px 透明 WebP，总计约 1.1MB
- [x] 两个新案例均保留分享设置和浏览器本机视频保存能力

## 发布与验收

- [x] 原子发布 `/opt/9face/releases/20260819100933`，保留 `/opt/9face/releases/20260819000208` 回滚点
- [x] 公网健康接口、两个 DEMO 清单、18 张新增 WebP 和新版前端资源均验收通过

## 当前生产

- 地址：`https://9face.mikeywa.site/`
- 主机：`115.159.25.169`
- 服务：`9face.service`，监听 `127.0.0.1:8095`
- 程序：`/opt/9face/current`
- 数据：`/var/lib/9face`
- 配置：`/etc/9face.env`
- 当前发布：`20260819100933`
- 回滚点：`20260819000208`

## 关键文件

- 前台：`src/generator/GeneratorApp.jsx`、`src/generator/generator.css`
- 本地拆图：`src/generator/localMatting.js`、`src/generator/matting.worker.js`
- 本机视频：`src/generator/localVideoRender.js`、`src/remotion/`
- 模型提示词：`server/pipeline.mjs`
- 团子重做脚本：`scripts/regenerate-tuanzi.mjs`
- 首页案例同步：`scripts/sync-home-examples.mjs`
- 服务：`server/index.mjs`
- 埋点存储与聚合：`server/analytics.mjs`
- 后台会话：`server/admin.mjs`
- 前端埋点：`src/analytics.js`
- 运营后台：`src/admin/AdminApp.jsx`、`src/admin/admin.css`
