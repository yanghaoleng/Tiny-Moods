# 设计规范

## 基调

- 关键词：Q 萌、手帐贴纸、轻产品感、克制、可信
- 参数：`DESIGN_VARIANCE 7`、`MOTION_INTENSITY 7`、`VISUAL_DENSITY 3`
- 字体：Nunito Variable；中文后备 PingFang SC / Microsoft YaHei
- 主色：粉 `#df557f`、墨 `#252024`、纸 `#f6f0ed`、黄 `#f3d585`、蓝 `#b7dbe1`
- 首页纯白，不使用渐变、几何底板或装饰性矢量图形

## 首页与打赏

- 导航右上角只保留浅灰药丸样式的“生成记录”；手机端只保留历史图标，避免导航换行
- 首页底部左侧“作者主页”沿用 NCM Studio：指向 `https://mikeywa.site`，使用 `13px` 灰色粗体、`14px` 外链图标，悬停上移 `1px`；页边距为顶部 `18px`、左右 `clamp(16px, 3vw, 36px)`、底部 `22px`
- 生成记录桌面端为居中双列面板，手机端为底部半弹窗；记录卡统一 `14px` 圆角，完成、处理中、失败仅用克制文字色表达状态
- 名字查询保留可见标签、输入框、查询按钮、加载/空/错误状态；已完成和等待前端处理的查询结果与我的记录复用同一作品卡

- 产品名统一为 `Tiny Moods`；页面标题、Canonical 和分享入口统一使用 `tinymoods.mikeywa.site`；首页主标题每次进入时从 `landingHeadlines` 的 11 句文案中随机选择一句，点击标题随机换一句且不连续重复
- 右侧以错位 2×2 摆 Jennie、团子、耙耙柑、羊石坨坨四个动态异形头像；每个头像下方是白底黑字播放按钮
- 首页上传按钮为无阴影扁平药丸，六种高对比彩虹色以 8.4 秒循环
- App Icon 旁使用首字母大写的 `Tiny Moods`，固定为同字号、同基线、`850` 粗细的 Cormorant Garamond 衬线字
- `Tiny` 四个字母固定黑底白字；`Moods` 五个字母使用红、丁香紫、绿、紫、黄、蓝、橙、粉随机独立配色
- 色块左右贴合字母宽度，上下按大写/上伸字母、x 高度字母、下伸字母三类边界裁切，形成参考图的阶梯外轮廓
- 字标以 `48ms` 字母错峰揭开，每 `3s` 只更新 `Moods`；悬停单字局部换色，点击整套重配
- 首页主标题使用 Typer 波浪入场：实心、反白、强调色、强调底和描边随机离散切换，相邻同状态自动合并，最终落回 `900` 粗体纯文字
- 主标题默认保持单行；仅当整句放不下时在第一个逗号后换行，换行后隐藏第一行末尾逗号，逗号前后两段内部不拆分
- 主标题容器使用 `translateY(10px)` 下移，不改变下方上传区的文档流位置
- 逗号任一侧达到 8 个字符时启用 `.is-compact`，确保 320px 手机端不横向溢出
- 自定义黑色鼠标指针使用 37px 图标，按下时仅向右下位移 2px；情绪轨迹标签保持原尺寸
- 低动态偏好下字标和主标题都直接静态展示
- 相机图标候选统一正放、无透视、真透明背景，在 `28-36px` 导航栏尺寸仍能看清机身和镜头
- 9 个方向依次为黑色彩圈、标题拼色、丁香拍立得、彩色光圈、珊瑚经典、薄荷玩具机、天空蓝、阶梯色块、黑白编辑感
- 图标只使用 `Tiny Moods` 现有黑、红、丁香、绿、紫、黄、蓝、橙、粉和暖白色板；不使用外投影、纸张出口或表情脸
- 进入或刷新页面时从 9 个图标中随机出现一个；点击整个 `Tiny Moods` 品牌区域切换到另一个，并同步重配 `Moods` 颜色；首页相机图标相对字标上移 `5px`，两者保持光学横向对齐
- 最近一次图标编号写入当前会话，刷新与连续点击都避免紧邻重复；页面不显示额外选择器
- 浏览器 Favicon 同步使用这 9 套相机色板；每次打开随机且不与上次紧邻重复，默认 SVG 回退图标使用“彩色光圈”方案
- 打赏层用一个双段 Switch 切换 `Lite / Pro`，默认 Lite；不展示模型说明卡和单独价格
- 模型区下面直接展示 `¥1 ~ ¥3`，去掉“建议打赏”字样；保留微信/支付宝双标签、二维码、关闭按钮和继续生成按钮
- 按钮明确写出当前模型，例如“我已打赏，用 5.0 Lite 生成”；打赏始终是自愿支持，不做收费墙
- 若 Lite 模型未开通权限，错误文案直接提示 Seedream 5.0 Lite 权限/配置问题，不再归因于照片清晰度，也不把 Lite 请求自动切成 Pro
- 二维码与继续生成按钮垂直间距为 `20px`，不把打赏做成收费墙
- 建议打赏固定为 `¥1 ~ ¥3`；下方文案为两行，“生成图片会产生费用，不需要给太多。”单独第一行，随机感谢语位于第二行；字号 `0.98rem–1.04rem`，每字经过 3 个 Typer 状态后落回纯文字
- 二维码下方使用低强度文字链接显示“有问题或建议请联系作者”，点击复制微信号 `yanghaoleng`
- 微信页签展示 `559×559` 原生赞赏码 JPEG；不裁切、不叠加 Logo、不再转码，与支付宝 WebP 分开管理

## 最终首页方案（2026-08-19）

- 固定使用“轻日系相册”，参数 `DESIGN_VARIANCE 5`、`MOTION_INTENSITY 3`、`VISUAL_DENSITY 2`
- 全站锁定明亮模式，不读取系统暗色偏好，不提供主题切换
- 首页上下布局，上传框在上、微浮动案例在下；单图拖拽/点击浏览，选图后展开对象名字输入框
- 首页导航下边线和案例区上边线均移除；上传框空态桌面高 `320px`、窄屏高 `270px`，选图预览同步减高，区块间留白也收紧一档
- 名字说明接在“它叫什么名字？”之后并位于输入框上方；输入框、移除照片、生成按钮统一 `54px` 高，在可容纳的窄屏继续同排对齐
- 首页标题下不放说明副标题，直接用留白过渡到上传框
- 案例角色使用透明头像与留白分组，不添加卡片、头像边框或底板
- 桌面案例为 3:4 居中浮窗，移动端为 82dvh 底部半弹窗；浮窗内隐藏互动页返回按钮，右上角统一关闭
- 分享二维码为 1080×1080 高纠错圆点码，点阵保持较小间隙；仅少量点使用深玫红、蓝、紫、青绿和陶土色，定位点改为同心圆
- 二维码中间使用带深色细边的暖白圆角正方形底板，透明人物表情完整置于底板内；不增加标题或海报装饰
- 打赏、二维码气泡和保存半弹窗继续使用极简黑白，童趣颜色只出现在二维码少量功能点上

## 生成素材约束

- 九宫格必须严格 3×3 三等分、零间距、零沟槽、零外边距，不画格子线
- 每格只出现同一主体的一颗头，从头顶到下巴；禁止脖子、肩膀、上身、衣服、手、手指和手臂
- 头部完整、大小一致、四边有单元格内留白；耳机必须无线、无连接线
- 浏览器先按母图三等分，再将每格四边内缩 `0.6%`；用于去掉格线残影，同时保留主体安全区
- 图片生成中小标题固定为“请不要关闭本页面”；完成后直接进入互动页，不出现成功中转页

## 互动页与视频

- 作品造型轮播背景固定在 5 个低饱和浅色中循环：浅粉 `#f8e9ee`、浅蓝 `#eaf4fa`、浅绿 `#edf6ef`、浅紫 `#f1edfa`、浅杏 `#fbf4df`
- 背景显示和装饰显示属于每个作品的独立持久设置；作品主人的更改保存到服务端，分享访客的更改只在当前页生效

- 互动页定向优化参数：`DESIGN_VARIANCE 8`、`MOTION_INTENSITY 6`、`VISUAL_DENSITY 4`，保留现有俏皮手帐语言
- 三行入场后从中心圆形打开；标题统一为“xxx的 / 9个 / 可爱瞬间”
- 主互动页顶部不显示作品标识和造型计数；左上角依次为房子主页按钮、背景音乐开关，顶部中间为背景与装饰开关
- 页面挂载后普通浏览器请求播放 BGM；微信内置浏览器先在封面中下方显示“播放音乐”胶囊按钮，拿到真实点击后再播放，状态以 `playing / pause / error` 为准；用户可随时用左上角开关暂停或恢复，手动暂停后不自动恢复
- 每约 6.563 秒换装；头像旋转缩放回弹约 0.6 秒；头像 Hover 不附加缩放或旋转，也不改变自动计时器
- Canvas 半调波纹只随头像切换触发，约 5.625 秒；不使用渐变、羽化光晕或 DOM 大圆
- 主体约为基础尺寸的 1.25 倍；沿 Alpha 通道使用轻量硬白边
- 每幕装饰最多三枚，物件贴纸 1～2 枚，其余为单色蜡笔笔触；不得遮挡主体
- 底部直接外露“复制链接 / 保存原图 / 分享二维码”，移动端使用短标签；声音只保留左上角背景音乐开关，底部不重复放界面音效按钮
- 二维码从底部操作栏上方以独立白色圆角小窗弹出，不使用指向箭头；二维码图片允许浏览器长按保存
- 保存原图打开底部半弹窗；弹层只展示裁剪和抠图前的完整九宫格，以及一个“下载 AI 原图”主按钮
- 原图下载直接返回火山引擎生成文件的原始字节，不在浏览器或服务端转码、拆分或打包
- 互动页不显示右下角 `made for` 角标，主标题已经足以说明作品归属
- 保存原图弹层底部两个操作不使用分割线；原图下载为填充主按钮，“保存视频（测试）”为无填充描边次要按钮
- 分享互动页左上角使用 Phosphor `House` 房子图标返回主页，不再使用 App Icon
- 纯白模式使用纯黑文字，不绘制点阵
- 点击保存视频进入本机渲染进度页；Loader 使用蜡笔涂鸦随机替换
- 视频布局 1080×1920，本机按 `2/3` 输出 720×1280、30fps、636 帧；优先 MP4

## 性能与兼容

- 首页不加载视频渲染器；用户点击保存后才动态加载本机渲染代码
- 九张图串行处理，一次只保留一格 RGBA 数据
- 半调背景单 Canvas，DPR 上限 1.5、约 30fps，页面隐藏时暂停
- DEMO 使用 720px 透明 WebP；Jennie 与羊石坨坨新增素材合计约 1.1MB，避免高清 PNG 解码峰值
- 首页案例只预取下一张表情，不再一次加载四组共 36 张；静态装饰、默认头像、Hero 和 App Icon 均转为 WebP
- Vite 指纹命名的 `/assets` 使用 `public, max-age=31536000, immutable`；构建后文件名变化自动绕过旧缓存
- 新永久作品同时保留火山原始九宫格和九张 1080px 透明 WebP；前者只用于预览和下载，后者用于互动页、二维码和视频
- `prefers-reduced-motion` 下关闭高强度动画
- 本机渲染失败时显示可读错误；完成后自动下载并保留“再次下载视频”按钮

## 运营后台

- `/admin` 是内部数据后台，不沿用首页的大幅动效；参数为 `DESIGN_VARIANCE 3`、`MOTION_INTENSITY 1`、`VISUAL_DENSITY 7`
- 登录页不显示账户名，仅保留 6 位密码框；手机优先唤起数字键盘，实体键盘支持数字输入和回车提交
- 使用明亮中性灰底、白色数据面和单一深玫红强调色 `#c83966`；状态色仅表达完成、处理中和失败
- 圆角规则：数据面 `14px`、记录卡 `12px`、按钮与输入 `9-10px`；不使用玻璃、发光或装饰性渐变
- 桌面端先展示核心指标、行为趋势和高频交互，再展示生成记录；每条记录直接预览 3×3 九图并显示永久链接
- 单次生成详情使用右侧抽屉，包含九图、用户上传原图、上传请求信息、生成参数、订单摘要、模型成本、失败详情和事件时间线
- “上传信息”使用后台同一套紧凑定义栅格展示：原图文件名、类型、大小、尺寸、IP、转发 IP、大致地址线索、设备/系统/浏览器、语言和来源页；原图预览下只提供一个“下载原图”文本入口
- 390px 手机端指标改为双列、筛选器单列、详情抽屉全宽；所有操作保持键盘焦点、错误态、空状态和加载态
- 后台不启用消费者首页的自定义鼠标、情绪轨迹、UI 音效或自动动画

## 界面音效

- 使用 `uisfx` 的 `10 Cinematic`，代码包名为 `cinematic`；总音量 `0.56`
- 选择、开关、打开、关闭、返回、复制、下载、重试、成功和失败均按语义触发，不播放悬停音
- 本机视频生成使用低音量 `processing` 循环，完成或失败立即停止
- 首次指针或键盘操作后解锁 Web Audio；静音状态保存到 `9face:ui-sfx`
- 音效只强化可见反馈，不替代文案、进度、错误态；与互动页 BGM 同时存在时保持低音量

## 鼠标情绪轨迹

- 仅精细指针设备使用 `37px` 近黑色大箭头，原生指针隐藏；按下时只向右下移动 `2px`，不缩放
- 轨迹标签为 `10px` 紧凑等宽字；单枚仅显示中文或英文一种语言，使用黑字、高饱和彩底和 `3px` 圆角
- 最快每 `88ms` 且移动至少 `26px` 才生成；固定复用 `10` 个标签节点，标签间保留至少 `4px` 空隙
- 标签总时长 `900ms`：约 `183ms` 淡入、停留约 `458ms`、约 `259ms` 淡出；只动画透明度与轻微缩放
- 页面隐藏时停止并清空；无精细指针设备完全隐藏，低动态偏好的桌面设备仅显示静态示例
- 性能约束：指针计算合并到单次 `requestAnimationFrame`；标签尺寸只在初始化时批量测量并缓存，移动热路径不读取布局；固定复用节点，移动箭头不使用实时滤镜
